# Hash-pinned atheris, installed by build.sh with --require-hashes. Every
# distribution PyPI publishes for this exact version is listed (the fuzz
# image's Python picks whichever wheel matches). atheris declares no
# dependencies of its own, so this one entry is the whole closure.
# To bump: update the version, then replace the hashes from
#   curl -s https://pypi.org/pypi/atheris/<version>/json
# (Dependabot's pip entry for /.clusterfuzzlite proposes the bump.)
# Held at 3.0.x: 3.1+ ships only cp312+ wheels, and the fuzz image's Python is
# 3.11 — see the `ignore` on that Dependabot entry.
atheris==3.0.0 \
    --hash=sha256:1f0929c7bc3040f3fe4102e557718734190cf2d7718bbb8e3ce6d3eb56ef5bb3 \
    --hash=sha256:510e502c57b6dc615fb174066407af620d4c7f73cf08a782c86e7761bf12c4eb \
    --hash=sha256:8a5c8a781467c187da40fd29139784193e2647058831f837f675d0bb8cbd8746 \
    --hash=sha256:a402cdca8a650d1371050b1f9552eb4cdc488d2db64950d603c4560318365eac
